{
  "name": "content-presenter",
  "version": "2.0.2",
  "description": "e-learning project: a library for presenting a course content",
  "main": "index.js",
  "scripts": {
    "transpile": "babel src -d dist && babel plugins -d dist && babel test/scripts -d test",
    "prepublish": "npm run transpile",
    "build": "rm -rf dist && npm run transpile && cp -r src/css dist/css",
    "start": "npm run build && webpack && node test/server"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/duongtdn/content-presenter.git"
  },
  "bugs": {
    "url": "https://github.com/duongtdn/content-presenter/issues"
  },
  "homepage": "https://github.com/duongtdn/content-presenter#readme",
  "author": "duongtdn",
  "license": "MIT",
  "peerDependencies": {
    "react": "^15.4.0",
    "react-dom": "^15.4.0"
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-plugin-transform-object-rest-spread": "^6.16.0",
    "babel-preset-env": "^1.6.1",
    "express": "^4.16.2",
    "react": "^15.4.0",
    "react-dom": "^15.4.0",
    "webpack": "^2.2.1"
  }
}
